Тест-кейсы для веб-приложения [ebookers](https://www.ebookers.com)
======================================

### test-case-1 ###
##### User sing in #####
- Предусловия:
  + Пользователь зарегистрирован, но не авторизирован в системе;
  + Пользователь корректно вводит свои "Email address" и "Password".
- Шаги:
  1. Пользователь открывает форму для авторизации нажатием *Accout -> Sign in*;
  2. Пользователь заполняет поля **Email address** и **Password**;
  3. Пользователь нажимает **Sign in**.
- Ожидаемый результат: *пользователь успешно авторизован в системе: переход на страницу поиска, в хедере есть надпись "Hello, [Имя пользователя]"*.

### test-case-2 ###
##### Simple hotel searching #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel only**;
  2. Пользователь корректно заполняет форму:
    + поле **Going to** значением "*Singapore, Singapore*";
    + поле **Check-in** завтрашней датой;
    + поле **Check-out** послезавтрашней датой;
    + поле **Rooms** значением "*1*";
    + поле **Adults (18+)** значением "*1*";
    + остальные поля остаются по умолчанию.
  3. Пользователь нажимает кнопку **Search**;
- Ожидаемый результат: *система выводит страницу со списком соответствующих введенным параметрам предложений, а именно:*
  + только отели Сингапура;
  + только комнаты, свободные для бронирования с завтрашней датой прибытия и послезавтрашней датой выезда;
  + количество доступных к бронированию комнат не менее 1;
  + на 1 взрослого человека.

### test-case-3 ###
##### Search hotel for less than one day staying #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel only**;
  2. Пользователь заполняет поле **Going to** значением "*Singapore, Singapore*".
  3. Пользователь заполняет поле **Check-in** послезавтрашней датой.
  4. Пользователь заполняет поле **Check-out** послезавтрашней датой.
- Ожидаемый результат: *система не позволяет установить такое значение для поля Check-out и моментально исправляет его значение на после-послезавтрашнюю дату*.

### test-case-4 ###
##### Search hotel with flight from A to A #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel only**;
  2. Пользователь корректно заполняет поля формы:
    + поле **Going to** значением "*Minsk, Minsk Region, Belarus*";
    + поле **Check-in** завтрашней датой;
    + поле **Check-out** после-послезавтрашней датой;
    + поле **Rooms** значением "*1*";
    + поле **Adults (18+)** значением "*2*";
    + отмечает checkbox **Add a flight**;
    + поле **Flying from** значением "*Minsk, Belarus (MSQ-Minsk Intl.)*";
    + остальные поля остаются по умолчанию.
  3. Пользователь нажимает кнопку **Search Hotel + Flight**;
- Ожидаемый результат: *система отображает страницу с сообщением "We were unable to find any flights for your package. Please adjust your search or continue booking your hotel separately."*.

### test-case-5 ###
##### Search hotel with empty form #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню);
  + Значения полей по-умолчанию.
- Шаги:
  1. Пользователь нажимает вкладку **Hotel only**;
  2. Пользователь нажимает кнопку **Search**;
- Ожидаемый результат: *система на той же странице отображает сообщение:*

  "*Please correct the errors below.*
  * *Please complete the highlighted destination field below.*"

### test-case-6 ###
##### Search hotel with empty check-in field #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel only**;
  2. Пользователь заполняет форму:
    + поле **Going to** значением "*Gothenburg, Sweden*";
    + поле **Check-out** завтрашней датой;
    + остальные поля остаются по умолчанию.
  3. Пользователь нажимает кнопку **Search**;
- Ожидаемый результат: *система на той же странице отображает сообщение:*

  "*Please correct the errors below.*
  * *Your length of stay cannot be longer than 28 nights.*"

### test-case-7 ###
##### Search hotel + car #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel + Car**
  2. Пользователь корректно заполняет форму:
    + поле **Going to** значением "*Minsk City Centre, Minsk, Belarus*";
    + поле **Check-in** послезавтрашней датой;
    + поле **Check-out** (послезавтрашней + 5 дней) датой;
    + поле **Rooms** значением "*1*";
    + поле **Adults (18+)** значением "*2*";
    + поле **Children (0-17)** значением "*1*";
    + остальные поля остаются по умолчанию.
  3. Пользователь нажимает кнопку **Search**;
- Ожидаемый результат: *система выводит страницу со списком соответствующих введенным параметрам предложений, а именно:*
  + только отели центра города Минска;
  + комнаты, свободные для бронирования с послезавтрашней датой прибытия и (послезавтрашней + 5 дней) датой выезда;
  + количество доступных к бронированию комнат не менее 1;
  + комнаты и машины позволяют разместить 2 взрослых и одного ребенка;
  + возраст водителей должен быть в отрезке [25, 70] лет.

### test-case-8 ###
##### Search hotel + car with little driver #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel + Car**
  2. Пользователь заполняет форму:
    + поле **Going to** значением "*Minsk City Centre, Minsk, Belarus*";
    + поле **Check-in** завтрашней датой;
    + поле **Check-out** (завтрашней + 3 дня) датой;
    + убирает галочку с checkbox-поля **Driver aged between 25 - 70?**;
    + поле **Driver's age** значением "*3*";
    + остальные поля остаются по умолчанию.
  3. Пользователь нажимает кнопку **Search**;
- Ожидаемый результат: *система на той же странице отображает сообщение:*

  "*Please correct the errors below.*
  * *Driver’s age must be between 18 - 99.*"

### test-case-9 ###
##### Search hotel with a large number of passengers #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel + Car**
  2. Пользователь заполняет форму:
    + поле **Going to** значением "*Bangkok, Thailand*";
    + поле **Check-in** завтрашней датой;
    + поле **Check-out** после-послезавтрашней датой;
    + поле **Rooms** значением "*1*";
    + поле **Adults (18+)** значением "*7*";
    + остальные поля остаются по умолчанию.
  3. Пользователь нажимает кнопку **Search**;
- Ожидаемый результат: *система отображает страницу с сообщением "Sorry, we’re unable to book more than 6 travellers. Please try a new search."*.

### test-case-10 ###
##### Search hotel at long time #####
- Предусловия:
  + Пользователь открыл форму параметров поиска **отелей** (*Hotels* в главном меню).
- Шаги:
  1. Пользователь нажимает вкладку **Hotel only**
  2. Пользователь заполняет форму:
    + поле **Going to** значением "*Minsk City Centre, Minsk, Belarus*";
    + поле **Check-in** послезавтрашней датой;
    + поле **Check-out** (послезавтрашней + 24 месяца) датой;
    + остальные поля остаются по умолчанию.
  3. Пользователь нажимает кнопку **Search**;
  - Ожидаемый результат: *система на той же странице отображает сообщение:*

    "*Please correct the errors below.*
    * *Dates must be no more than 28 days apart.*"
